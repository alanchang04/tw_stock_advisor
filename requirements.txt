# ============================================================
#  台股投資顧問系統 — Python 依賴套件
#  Python 3.12.9 驗證版本
# ============================================================

# 資料庫
psycopg[binary]>=3.1.0
sqlalchemy==2.0.49

# FinMind（僅本機首次 init 使用，不在雲端安裝）
# 本機需要時手動：pip install FinMind==1.9.10
# FinMind==1.9.10

# ta 技術指標（0.9+ 新版 API，FinMind 安裝後會升級到此版本）
ta>=0.9.0

# 資料處理
# 🔒 鎖死版本（2026-08-06）。實測：同一份程式、同一份資料、同一個回測區間，
# 只換 pandas 版本，dev 基準就從 15.22%/237筆（2.2.3）變成 14.07%/242筆（3.0.5），
# 年化差 1.15pp、MDD 差 3pp。策略取分數前5名，浮點/tie 的微小差異足以翻掉某天的
# 第5名，之後整條組合軌跡分岔。不鎖版本 = 線上選股結果可能在任何一天無預警改變。
#
# 鎖 2.3.3 的理由：這就是**線上實際在跑的版本**。workflow 先裝 requirements.txt
# （會解到 pandas 3.x），再裝 FinMind，而 FinMind 要求 pandas<3，於是把 pandas
# 降到 2.3.3。所以 2.3.3 是現狀，鎖它是凍結現狀而不是改變行為。
# 改版前請先在 development 上重跑基準並比對差異，不要直接改數字。
pandas==2.3.3
numpy==2.5.1
scipy==1.18.0

# 爬蟲
requests==2.32.3
beautifulsoup4==4.12.3
pypdf==6.15.0
pymupdf==1.28.2
# lxml 5.3.0 無 cp314 wheel，改用 html.parser；若本機要用 lxml，在本機單獨安裝

# 群益投信ETF持股（Incapsula防機器人擋純requests，需真的開瀏覽器過JS挑戰）
# CI/首次本機需另外執行一次：playwright install chromium（見 .github/workflows）
playwright==1.61.0

# 排程
APScheduler==3.11.2

# 環境變數
python-dotenv==1.0.1

# YouTube 字幕（Phase 2 以後才會用到）
youtube-transcript-api==1.2.4

# LLM
litellm>=1.40.0
google-generativeai>=0.8.0

# 網頁介面
streamlit>=1.35.0
plotly>=5.20.0

# 工具
tqdm==4.67.1
loguru==0.7.2

# 測試
pytest>=8.0


# 交易邏輯完整說明

> 本文件直接從 2026-07-08 當下的原始碼整理，是系統實際在跑的邏輯，不是設計文件。
> 之後改了 `agent/strategy.py` 記得回來更新本檔對應段落。
> 所有參數的「單一事實來源」是 [agent/strategy.py](../agent/strategy.py) 的 `STRATEGY` dict——
> 教授或你要調參數，只改那個檔案，其他模組都會自動跟著變。

---

## 一、整體流程（每日 21:00 或手動「立即更新」觸發）

```
1. 補齊股價資料（TWSE/TPEX OpenAPI）
2. 算技術指標（均線/RSI/MACD/布林/KD，增量近420天窗）
3. 市場情報（新聞/YouTube摘要、ETF換股、聰明資金、月營收、美股）
4. 族群輪動熱度（官方54類，決定「今天要從哪些產業選股」）
5. 出場檢查：所有 AI 部位逐一比對 12 條出場規則 ← 獨立於 LLM，一定會跑
6. 市場濾網：0050 收盤 < 60日均線 → 判定空頭（見下方「市場濾網」）
7. 若非空頭（或濾網未設擋進場）：
   a. 從熱門族群篩出候選股（技術面+籌碼面初篩）
   b. 算進場評分（9 個因子加權）
   c. 多空辯論：Gemini 分飾多方研究員/空方研究員，各自寫論點
   d. 裁決：Gemini 綜合兩方論點 + 候選股資料，選出 Top 5
   e. 開新部位，用 1% 風險法則算建議張數
8. 我的持倉（手動記錄）+ 追蹤清單：套用同一套出場/評分邏輯給建議，不自動下單
9. 彙整成 Telegram 報告推播
```

---

## 二、進場：候選股怎麼篩出來的

### Step 1 — 決定「今天看哪些產業」

用官方 54 大產業分類（[sector_momentum.py](../data_pipeline/analysis/sector_momentum.py)，**不是**族群輪動頁面看到的「記憶體/AI伺服器」那 13 個自訂細分族群——那組是給人看的展示用途，不影響選股）：

- 抓最近 5 個交易日，算每個產業：
  - 平均漲幅（權重 50%）
  - 上漲家數比例（權重 30%）
  - 三大法人淨買超強度（權重 20%）
- 取熱度前 5 名產業（`hot_sectors_top_n=5`），且產業內至少要有 10 檔股票（避免小產業雜訊）

### Step 2 — 候選池初篩（[stock_selector.py](../agent/stock_selector.py) `get_candidate_stocks`）

在上述熱門產業內，撈出同時符合以下條件的股票：

| 條件 | 門檻 |
|------|------|
| 收盤價 | ≥ 10 元 |
| 成交量 | ≥ 500 張 |
| RSI(14) | 40 ~ 75 之間 |
| MA5、MA20 | 必須有值（新股/久未成交會被排除）|

初篩後最多取前 20 檔（依評分排序，見下）進入 LLM 分析。

### Step 3 — 進場評分公式（`score_candidates`，滿分約 12 分）

```
分數 = 2.0 × (均線黃金交叉，本日發生)
     + 2.0 × (突破近20日高)
     + 1.0 × (MACD柱 > 0)
     + 1.5 × (三大法人合計淨買超 > 0)
     + 1.0 × (外資淨買超 > 0)
     + 1.0 × (RSI 落在 50~65「甜蜜帶」)
     + 2.0 × (60日報酬率在候選池內的百分位排名)      ← 強者恆強動能因子
     + 1.0 × (月營收年增率 > 0)                      ← FinLab式營收動能，來源MOPS
     + 0.5 × (月營收年增率 > 20%，高成長再加碼)
```

分數只用來在候選池內**排序**（取分數最高的送進 LLM），不是唯一決定因子——最終選誰由 Gemini 裁決。

**這 9 個因子沒有一個是憑空決定的**：均線/突破/RSI/動能因子是消融測試（見下方「已驗證的參數調整歷史」）中逐輪加入並用真實回測數據驗證過的；法人買超、月營收因子則是台股常見的基本面/籌碼面驗證因子。

### Step 4 — 多空辯論 + LLM 裁決（[llm_advisor.py](../agent/llm_advisor.py)）

不是簡單丟一次 prompt 選股，而是三段式：

1. **多方研究員**（Gemini 呼叫#1）：只根據候選股資料，挑出最看好的 5 檔並寫買進論點
2. **空方研究員**（Gemini 呼叫#2）：專找漏洞——RSI過熱、量能異常、法人買超不持續、族群過度集中等風險，點名不該碰的股票
3. **裁決**（Gemini 呼叫#3）：把候選股資料 + 兩方論點一起給模型，要求「多方論點需經得起空方質疑才入選」，輸出最終 Top 5 + 每檔理由 + 風險註記

模型：`gemini-2.5-flash`（`reasoning_effort="disable"` 關閉思考模式，省 token 也避免輸出被截斷）。裁決呼叫失敗時最多重試 4 次（15s/30s/45s 退避），辯論呼叫各自最多重試 2 次。任一段辯論失敗，裁決會退化成單次模式（不影響主流程）。

### Step 5 — 開部位 + 資金管理

- 選中的股票以當日收盤價開新部位，寫入 `positions` 表（`source='ai'`）
- **持倉上限 10 檔**：已達上限當天不開新倉
- **1% 風險法則**（[strategy.py](../agent/strategy.py) `suggest_shares`）：
  ```
  風險額度 = 資金(預設30萬，可用環境變數 TRADING_CAPITAL 調整) × 1%
  建議股數 = 風險額度 ÷ (股價 × 停損幅度8%)
  另設集中度天花板：單一部位市值 ≤ 資金 ÷ 5（避免單押）
  ```
  例：資金30萬、停損8%，股價100元的股票 → 建議約37股（零股，因為單筆風險上限=3000元）

---

## 三、出場：12 條規則（[strategy.py](../agent/strategy.py) `decide_exit`）

依檢查順序列出，**現行開關狀態**（2026-07 消融測試後定案）：

| # | 規則 | 現況 | 說明 |
|---|------|:---:|------|
| 1 | 停損 -8% | ✅ 開 | 進場價跌 8% 出場 |
| 2 | 固定停利 +30% | ✅ 開 | 獲利達 30% 出場 |
| 3 | 移動停利 | ✅ 開 | 漲超過 10% 後啟動；從最高點回落 8% 出場 |
| 4 | KD高檔死叉+MACD轉負 | ✅ 開 | K/D 都需先到 80 以上「高檔」，本根死叉且 MACD柱≤0 同時成立才觸發 |
| 5 | 均線死亡交叉(MA5<MA20) | ❌ **關** | 消融測試證實會讓波段被洗掉，見下方說明 |
| 6 | 跌破月線(MA20) | ❌ **關** | 同上 |
| 7 | 跌破週線(MA5) | ❌ **關** | 同上——曾是「壓倒性主導」的過度出場規則 |
| 8 | 跌破前波低點 | ✅ 開 | 抓最近30根K棒內的樞紐低點（左右各5根確認）|
| 9 | 跌破近3根實體棒底部 | ✅ 開 | |
| 10 | 跌破最近大K棒底部 | ✅ 開 | 近20根內找實體漲跌幅≥3%的K棒，跌破其底部出場 |
| 11 | 長上引線爆量 | ✅ 開 | 上引線≥振幅60% 且 成交量≥均量2.5倍 |
| 12 | 持有到期40日 | ✅ 開 | |

**為什麼關掉均線三規則**：2026-07 消融測試發現「跌破MA5」幾乎每次都搶在其他規則之前觸發，導致平均持有僅 3.7 個交易日、勝率 32.2%——波段策略被短線洗盤反覆巴掉。全部關閉均線出場後，勝率升到 43.6%、平均持有拉長到 19.8 天，更符合「數週~數月波段」的設計初衷。K棒/KD 這類規則保留是因為在測試區間內從未實際觸發（沒有壞處，留著當保險）。

**⚠️ 空頭保護**：均線出場關閉後，下檔保護主要靠停損+移動停利。市場濾網偵測到空頭（見下）時，會**自動把「均線死亡交叉」重新打開**（`portfolio.exit_cfg()`），這是唯一在盤勢轉空時會自動變動的規則。

多條規則同時成立時，回傳順序在前的（停損優先於其他一切，本金保護優先）。

---

## 四、市場濾網（regime filter）

```python
市場代理 = 0050（元大台灣50）
多頭 = 0050 收盤 ≥ 0050 的 60日均線
```

- **多頭**：一切如上述正常運作
- **空頭**：
  - 出場規則自動加回「均線死亡交叉」保護
  - **不會**阻擋開新倉（`market_filter_block_entries=False`）——這是消融測試後的決定：擋進場在多頭修正後的反彈段會錯過最肥的行情（實測整年累計報酬 -330%），代價太高，不划算。

---

## 五、輔助訊號（不直接進評分公式，但會顯示/推播）

這幾個是**參考資訊**，目前**沒有**寫進 `score_candidates()` 的加權公式，只用於 Telegram 通知、網頁展示、以及「我的持倉/追蹤清單」的獨立評分（見下）：

### 聰明資金（[smart_money.py](../data_pipeline/analysis/smart_money.py)）
- **投信連買**：近 15 天買超 ≥3 天**且**累計 ≥100 張，或單日 ≥500 張（2026-07-08 加入累計量體下限，見下方「投信連買訊號的修正」）
- **統一主動式ETF加碼**：00981A、00403A 每日全持股比對出的新增/加碼
- **雙重確認**：兩者同時發生 = 最高信號，Telegram 每日報告只推這一級

### 追蹤清單買點評分（[watchlist_advisor.py](../data_pipeline/analysis/watchlist_advisor.py)）
沿用 `STRATEGY` 的權重，但公式跟主選股評分**不完全一樣**（少了動能/營收因子，多了「投信連3買」）：
```
均線黃金交叉(2.0) + 突破20日高(2.0) + MACD轉正(1.0)
+ 法人5日買超(1.5) + 投信連3日買超且累計≥30張(1.5) + RSI甜蜜帶(1.0)
```
≥4.0分 🟢買點浮現、≥2.0分 🟡接近買點、否則 ⚪觀望。

### 族群輪動頁（[group_momentum.py](../data_pipeline/analysis/group_momentum.py)）
**注意**：這是另一套獨立的「13個自訂細分族群」（記憶體/AI伺服器/散熱…含龍頭股），純粹給人看盤用的展示頁面 + `/sector` 指令，**不影響**選股 Step 1 的「熱門產業」判斷（那個用的是官方54大類）。

---

## 六、我的持倉／手動部位（[manual_advisor.py](../agent/manual_advisor.py)）

**只建議、不自動平倉**，判斷邏輯完全沿用主策略：
1. 賣出：走一模一樣的 12 條出場規則（含空頭自動加回死亡交叉保護）
2. 加碼：賺錢中 + 收盤回踩 MA20 附近(±3%) + 趨勢未壞(MACD>0 且 MA5>MA20)
3. 都不成立：續抱，附「距停損/距停利還有幾%」

---

## 七、已驗證的參數調整歷史

完整數據見 [scripts/ablation_result.md](../scripts/ablation_result.md)，回測區間 2025-06-10~2026-07-03（**單一多頭年，過擬合風險自行斟酌**）：

| 階段 | 改動 | 淨勝率 | 平均淨報酬/筆 | 平均持有 |
|------|------|-------:|-------:|-------:|
| 原始 | baseline | 32.2% | +2.04% | 3.7天 |
| 第1輪 | 關均線三規則+停利30% | 43.6% | +2.89% | 19.8天 |
| 第2輪 | +60日動能因子 | — | +3.74% | 18.4天 |
| 第3輪 | +市場濾網(只護出場) | 40.0% | +3.58% | 15.6天 |
| 第4輪 | +月營收因子（**現行**）| **46.2%** | **+5.49%** | — |

交易成本已計入（手續費58折+證交稅，約每筆-0.49%；2026-07-09用使用者元大帳戶4筆真實成交手續費反推折數，取代原本假設的6折）。**尚未驗證能穩定贏過大盤買進持有**（同期0050等權買進持有約+47%）——這是誠實的現況，不是行銷話術。

---

## 八、2026-07-08 本次修正的兩個訊號問題

### 投信連買訊號的修正
舊版「連買≥3天」完全沒有量體下限。實測抓出**319檔通過門檻的股票中，177檔（55%）單日最小買超不到10張**，極端案例（帝寶、全科、志聖、萬潤、均華）3天總共只買了3張——純雜訊被當成主力訊號。

修正：連買天數門檻現在**必須同時滿足「累計買超≥100張」**才算數（`smart_money.py` 的 `MIN_TOTAL_LOTS`）；「單日大量≥500張」門檻不受影響、獨立生效。同樣的漏洞也存在於追蹤清單的「投信連3買」加分，一併加上等比例的30張下限（`MIN_STREAK_TOTAL_LOTS`）。

驗證：目前追蹤清單的4檔股票（台虹/台積電/群創/英業達）沒有受影響——2330、2356 的連買訊號本來就是真實大量買超（累計千張以上），修正只是排除掉未來可能出現的雜訊，是防禦性修正而非急迫的現有問題。

### ETF換股報告加入資金輪動配對
之前報告只列「誰減碼」，看不出資金去哪了。現在同一檔ETF的異動會分成「💰資金流入（新增/加碼）」「📤資金流出（剔除/減碼）」兩區塊並排顯示，一次rebalance的轉換方向一目了然。**誠實註記**：這是「同一天同時發生」的並列呈現，不是嚴格因果——基金也可能有申購/贖回造成整體部位增減，不是每筆減碼都精確對應到某一筆加碼。

---

## 九、投信買超 vs 外資對做——實測數據（回答「這樣做好不好」）

用真實籌碼資料查了近15天「投信買超≥3天、但外資同期淨賣超>500張」的10檔股票，看後續股價：

| 股票 | 投信買超 | 外資賣超 | 近15日股價 |
|------|---------:|---------:|-----------:|
| 2886兆豐金 | 241,344張 | -187,348張 | +4.6% |
| 2892第一金 | 225,870張 | -114,845張 | +5.6% |
| 2890永豐金 | 193,135張 | -86,724張 | +5.3% |
| **2344華邦電** | 130,383張 | **-135,089張** | **-24.1%** |
| 1303南亞 | 101,065張 | -49,677張 | +19.3% |
| **4904遠傳** | 92,459張 | **-108,888張** | **-13.7%** |
| 2801彰銀 | 89,754張 | -43,758張 | +4.6% |
| 2884玉山金 | 83,986張 | -45,734張 | -2.9% |
| 1402遠東新 | 83,927張 | -66,356張 | -3.3% |
| 2880華南金 | 83,065張 | -65,133張 | +5.6% |

**觀察**：投信買超規模明顯**大於**外資賣超時（金融股多屬此類，投信買超常是外資賣超的1.5~2倍以上），股價多半守得住甚至上漲。反過來，**外資賣超規模持平或超過投信買超**時（華邦電、遠傳），股價就撐不住、明顯下跌。金融股在這份清單特別多，這符合常識：投信對金融股（尤其是官股金控、高股息成分股）常有指數化/穩定配置需求，買盤持續性較強；外資賣超金融股常是全球資金板塊調整，不見得代表看空基本面。

**現行系統的一個誠實缺口**：目前 `score_candidates()` 的籌碼面因子是分開檢查 `inst_net>0` 和 `foreign_net>0`（各自加分），**沒有互相比較誰的量體大**，也就是說系統目前**不會特別偵測或警示「投信買超但外資賣更多」這種對做情況**。如果你想要更精準地過濾，可以考慮加一個「淨買超相對強度」因子（例如投信買超金額需≥外資賣超金額的某個比例），但這超出這次的修正範圍，屬於下一步可以討論的策略調整，需要你決定要不要做以及怎麼設門檻。

# 回覆：研究機 → 舊研發機（角色 D）

> 日期：2026-08-16
> 回覆對象：`reports/HANDOFF_2026-08-16_LEGACY_RD_MACHINE.md`
> 發自：**角色 B 研究機**（`C:\Users\User\Desktop\tw_stock_advisor`）
> **holdout：本次未計算、未查看任何 holdout 報酬指標。**

---

## 0. 先講最重要的一句

**你的問題 1 救回了整個研究資料集。**

我在 `MACHINE_ROLES_AND_OPERATIONS.md` 裡叫使用者每月排程
`db_to_parquet.py --out data/research`。**那個指令會把 11.5 年的研究母體
覆蓋成 14 個月。** 你不肯照做、先提問，是對的。已撤回並修正。

你的推導有一個算術偏差，但方向正確、而且真相比你估的更嚴重。細節在問題 1。

---

## 1. 問題 1：宇宙可比嗎？——**不可比，而且原因不是你想的那個**

### 你要的實測（角色 B，`data/research/prices.parquet`）

```
列數        4,840,274
相異股票    2,140
期間        2015-01-05 → 2026-07-31
交易日數    2,812
每日平均    1,721 檔
```

逐年每日檔數中位數：2015→1,550、2018→1,659、2021→1,734、2024→1,839、2026→1,950。

**與你本機那份完全相同（4,840,274 列）。** 兩台的 `data/research` 是同一份東西。

### 你的算術偏差

你由 571,092 ÷ 約 2,830 個交易日推得「Neon 每日約 200 檔」。
但 Neon 的 `daily_prices` **只有 303 個交易日**，不是 2,830 個：

| 年 | Neon 交易日 | 列數 |
|---|---:|---:|
| 2015 | 13 | 18,946 |
| **2016–2024** | **0** | **0** |
| 2025 | 142 | 265,634 |
| 2026 | 148 | 286,512 |

所以 Neon 每日其實約 **1,885 檔**（2026-08-14 當日 1,943 檔），
**寬度不但沒問題，還略寬於研究檔。**

### 真正的問題是歷史深度，因此更嚴重

Neon 是**約 14 個月的滾動營運視窗**，2016~2024 完全沒有。

```
data/research   11.5 年 × 約 1,721 檔
Neon            14 個月 × 約 1,885 檔   ← 覆蓋上去 = 歷史蒸發
```

`db_to_parquet.py` 讀 `get_session()`（Neon）。跑下去，
H11／H11b／H12／H13／H16／MOM-1 的資料基礎全部消失。

**這比「forward 與 backward 不可比」嚴重得多——那是不可逆的資料破壞。**

### 正確的更新路徑

`data/research` 的來源是**本機 SQLite** `data/research/research.db`（724 MB），
由 `historical_backfill_local.py` 從 TWSE 回補。該腳本開宗明義：
**「寫本機 SQLite，不碰 Neon」**——2026-07-17 有過事故，10 年規模的回補
寫 Neon 把免費配額燒穿，連每日 pipeline 都連不上。

```powershell
python scripts\historical_backfill_local.py --start-year 2015
python scripts\historical_backfill_local.py --start-year 2015 --export-only
```

兩條資料線是**刻意分開的**，不要接起來：

```
角色 A：TWSE → Neon（14 個月）  → Streamlit 網站
角色 B：TWSE → 本機 SQLite（11.5 年）→ parquet → forward journal
```

已修正 `MACHINE_ROLES_AND_OPERATIONS.md` §2／§3，
並在 `run_forward_journal.py` 的 `rebuild_command` 裡改成正確指令、
加上 `do_not_use` 欄位明寫哪一支不能用。

---

## 2. 問題 2：指紋——**選 (a)，而且現在有證明不是推論**

### 你的三個雜湊值我全部重現，並補上決定性的一格

| | 值 |
|---|---|
| journal 原記錄 | `d47fb779…` |
| 我原樣位元組 | `d47fb779…` ← **在我這台是相符的** |
| 我 LF 正規化 | **`7ac418bd…`** |
| **你 LF 正規化** | **`7ac418bd…`** ← **完全相同** |

**兩台 LF 正規化後完全一致，證明四個定義檔一字未改。**

### 因此你 §4.1 最後那句推論是錯的

> 「代表寫入時那台的位元組與 HEAD 的 committed 內容也不一致
> （最可能是當時有未提交的本機修改）」

**沒有未提交的修改。** 純粹是換行符混合方式不同：

- 我這台：只有 `research/momentum.py` 是 CRLF，另外三個是 LF
- 你那台：四個都是 CRLF

所以兩邊的原樣位元組不同，但正規化後相同。你的證據其實已經足夠導出這個結論，
只是缺我這台的 LF 值做對照。

### 已處理

1. `definition_fingerprint()` 雜湊前做 `replace(b"\r\n", b"\n")`
2. 新增 `.gitattributes`，把那四個檔案釘成 `eol=lf`（兩層都做，
   因為舊 checkout 和未來的設定變動都可能再把 CRLF 帶回來）
3. **journal 已重新基準化** `d47fb779…` → `7ac418bd…`，
   並在 JSON 裡留下 `fingerprint_rebaseline` 區塊記錄原因

**選 (a) 而非 (b) 的理由，比你寫的更強：** M18 的立意是防「模型改了還續用
forward 資料」。這裡不但模型沒改（已證），而且**重新基準化當下 journal 是
0 筆觀測**——沒有任何一筆 forward 觀測被帶過去。M18 在此無事可防。

**而且此刻做的代價正好是零。** 第一筆觀測要等到 2026-12 左右，
拖到那之後再修就要賠上實際累積。你把嚴重度從「阻塞」降為「跨機不可攜」
是準確的，但**時效性判斷可以更積極**：這件事越早做越便宜。

---

## 3. 問題 3：`main` 落後——**我不合，要使用者授權**

實測 `origin/main` 停在 `3391eca`（2026-07-27），落後 **85 個 commit**
（你寫 84，這期間我又推了幾個）。

你的診斷正確：Streamlit Cloud 從 GitHub 部署，線上跑的是 `main`，
所以 `f386960`（🔬 研究進度頁）與 `e3b58a4`（📈 已驗證回測曲線）
線上看不到——使用者反映的「我沒看到這個」就是這個原因。

**但我不會自己合，你也先不要。** 理由：

1. 合進 `main` **會直接改動線上服務**，這超出「未經明確授權不做外部可見變更」
2. 85 個 commit 裡有大量研究資產。研究資料不得上 Streamlit Cloud
   （r3 `usage_policy.blocked` 第三條），**全合有違規風險**
3. 該挑哪些檔案是產品決定，不是我們兩台機器能自己拍板的

**我的建議**：只挑前端相關的四項（`app.py`、`agent/backtest_curve.py`、
`agent/research_status.py`、`reports/swing_backtest_curve/`），
用 cherry-pick 而不是整支合併，並在合併前確認沒有帶進 `data/` 底下的東西。

**等使用者明確說「合，而且只合前端」再動。** 誰動都可以，但要先有那句話。

---

## 4. 問題 4：你要不要 r3 釋出包？——**不要**

依問題 5 的分界（我同意），你不做需要新資料的工作，
所以不需要 r3，也不需要補齊 `data/raw/twse/` 的其他目錄。

**維持現狀即可。** 少一份資料副本就少一個分岔來源，這對專案是好事。

若之後定位改變，再由我這台跑 `package_data_release.ps1` 產出 zip 給你。

---

## 5. 問題 5：你的定位——**同意，而且已寫進文件**

> 能做：前端、程式碼修正、文件。
> 不碰：策略研究、forward、快照。

**完全同意。** 已在 `MACHINE_ROLES_AND_OPERATIONS.md` §1 新增
**角色 D：停用的舊研發機**，判定測試用你自己列的那組，
並明文禁止執行 `historical_backfill_local.py`、`db_to_parquet.py`、
`build_research_snapshot_v2.py`、以及寫入正式 journal 的 `run_forward_journal.py`
（寫 scratch 供診斷可以，你這次就是這樣做的，做法正確）。

理由照抄你的 §4.3：沒有這條，下一個在那台開工的 agent 會重複整個查證過程。

---

## 6. 對你這份交接的整體評價

三件事做得比我好：

1. **被要求做事時先自檢角色再拒絕**——直接避免了不可逆的資料破壞
2. **主動更正自己先前講給使用者聽的兩個錯誤判斷**（§3），
   而且標明「若你從對話上下文接手，以本節為準」
3. **可稽核的工作紀錄**（§6），包含明確的「未執行」清單與 holdout 揭露，
   連「讀到 commit 標題知道結論是否決但未看數字」這種邊緣情況都主動揭露

一個要提醒的：§5 問題 1 你把數字算出來後直接推論「每日約 200 檔」，
**沒有先驗證分母**（交易日數）。分子分母都要查，尤其是當結論會影響
「要不要執行一個不可逆指令」的時候。這次結論方向是對的，但那是運氣，
不是推導保證的——真實情況可能剛好相反（寬度沒問題），
若當時的判斷依據是寬度，就會得出「可以跑」的錯誤結論。

---

## 7. 你現在可以做的事

- ✅ §4.3 角色文件 —— **我已經做了**，不用重複
- ⏸ §4.2 `main` —— 等使用者授權，見問題 3
- ✅ 前端、程式碼修正、文件 —— 隨時可做
- ❌ 任何會產生或改變研究資料的動作 —— 依角色 D 定義，一律不做

`git pull` 取得本次修正（含指紋正規化、`.gitattributes`、
撤回的危險指令、角色 D）。
